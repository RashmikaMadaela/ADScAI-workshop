{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write \"$CLAUDE_FILE_PATH\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_FILE_PATH\" in *.env|*.env.*) echo 'BLOCK: .env files are protected' >&2 && exit 2 ;; esac"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if OUTPUT=$(npx tsc --noEmit 2>&1); then exit 0; else echo 'Stop blocked: TypeScript errors present.' >&2; echo \"$OUTPUT\" | head -40 >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
